# A.R.C. project status — 2026-04-25

A snapshot of where every major piece sits. **Read this before deciding what to work on next.** Three columns:

- **🟢 In the live game** — wired into the Phaser scene, gameplay can hit it from a real user session.
- **🟡 Mockup-only** — painted/built as static HTML under `apps/game/public/admin/`, design preview only, not yet wired into Phaser gameplay.
- **🔴 Missing / not started** — known design work yet to begin.

## Auth + onboarding

| Feature | Status | Notes |
|---|---|---|
| Welcome screen | 🟢 **Live** | Iframe-loaded by `AuthOverlay.ts`. Painted full A.R.C. logo on the wooden sign, sleeping cat with snoring ZZZ animation, wagging-tail golden retriever, fox cameo, painted garden background. |
| Sign-up flow | 🟢 **Live** | Two stages (name+animal picker → 4-digit PIN keypad). Adaptive avatar grid (8 animals), keyboard PIN input, painted red error sticker on validation failure, ZZZ + tail-wag animations consistent. |
| Login flow | 🟢 **Live** | Recent-users avatar grid as primary CTA; `Don't see your face? Type your name` link as secondary fallback (adaptive based on chip presence). PIN keypad on stage B. |
| Supabase auth back-end | 🟢 **Live** | Real signup/login. Username min-length lowered to 2 (was 6) so kid-names like Lily / Ben / Eve work. |
| Auth-overlay scene-switching | 🟢 **Live** | `AuthOverlay.ts` orchestrates iframe pages via `postMessage`. |

## Driving system (PTV / Supply Run)

| Feature | Status | Notes |
|---|---|---|
| First-drive vet-run flow (slice C) | 🟢 **Live** | `drive-overlay.html` intercepts the existing vet flow on the first sick animal. Three stages: CTA → ~10s scripted painted drive → arrival at Bay Road Vets. Hands off to the existing vet popup. |
| Cockpit layout / dashboard | 🟡 **Mockup** | `cockpit.html` rebuilt to real-car layout (horn in wheel hub, speedo above wheel red-when-speeding, brake-left-accel-right pedals below, ignition with key-turn or button per vehicle, hazard button, dash-mounted GPS). 5 per-vehicle dashboards. |
| Per-vehicle painted art | 🟡 **Mockup** | Henry painted dashboard wired in. 5 painted vehicle sprites + 5 painted mirrors + 17 painted charms staged at `apps/game/public/assets/driving/` and being wired into mockups by parallel agent (in progress). |
| Pre-drive screen | 🟡 **Mockup** | `pre-drive.html`: destination → vehicle → mode → cargo-load → "Let's go!". |
| GPS world map | 🟡 **Mockup** | `map.html` rebuilt with painted Birchie geography, zoom-based pins, longer-stem signposts. Not yet loaded by InGameOverlay. |
| Cockpit slot-map editor | 🟡 **Mockup** | `cockpit-editor.html` — drag/resize 11 interactive slot bounding boxes + 2 polylines (dash-top, dash-bottom). Per-vehicle tabs with localStorage. PNG mask export ready for OpenAI in-paint. |
| OpenAI in-paint pipeline | 🟢 **Tool ready** | `tools/paint-dashboard.ts` CLI: takes a slot-mask PNG + per-vehicle prompt, calls `/v1/images/edits`, outputs aligned painted dashboard. Tested. |
| Supply Run mode | 🟡 **Mockup** | Cockpit toggles to a daylight racing-fun palette (NOT neon/Blade Runner). Music brief: heavy metal Metallica-style — placeholder still. |
| Hedgehog-crossing mechanic | 🟡 **Mockup** | Universal across PTV + Supply Run — flashing red triangle warning + text prompt. Cockpit drill works in mockup. |
| Real-time city + fake-3D road | 🟡 **Mockup placeholder** | SVG scenery sprites scroll past at speed. UK-spec two-lane road, oncoming traffic + slow vehicles to overtake. Painted hedgerow + roadside-sprite art still needed. |
| Cargo crate-stacking puzzle | 🟢 **Logic live** | `crate-stacking.ts` engine + 32 unit tests. UI surfaces in pre-drive mockup but not yet in real game. |
| Cargo-comfort meter + animal mood | 🟢 **Logic live** | `mirror-mood-sprites.ts` + 28 mood composites (cat/dog/bunny/fox/bat/parrot/snake × happy/neutral/stressed/sleeping). Renders inside the mirror in cockpit mockup. |
| Charm system | 🟢 **Logic live** | `charms.ts` + 20 unit tests, 17 charms with unlock predicates. 17 painted charm PNGs delivered. UI selection panel not yet built. |

## Cast + adopters

| Feature | Status | Notes |
|---|---|---|
| 32 adopter household portraits | 🟢 **Live** | `apps/game/public/admin/scene-assets/cast/01–32-*.png` painted-storybook portraits. Used in adopters list, arrival popups, return visits. |
| Apprentice cast (Rhubarb / Amara / Kofi) | 🟢 **Live** | Standing portraits + multiple action variants. |
| Apprentice walking poses | 🟢 **Live** | 9 sprites (front / side / waving × 3) via OpenAI gpt-image with reference-portrait conditioning. Stronger style anchor (Julia-Donaldson, no anime) after v1 drift. |
| Adopter walking poses | 🔴 **Reverted** | Wave-1 (30 sprites) generated but reverted — multi-person households drifted hard (triptych compositions, identity loss, extra people). Plan: per-person sprite approach for wave-2, not per-household groups. |
| Cast static portraits used as walking-cameo placeholders | 🟢 **Live** | Per Marcus's directive — adopter cameos in-game use the existing standing portraits; no walking animation until wave-2 is re-attempted with the per-person approach. |
| 4 cropped portraits regen | 🟢 **Live** | 07-patel-greens / 12-odenkirks / 28-iris-teacher / 17-benji-wild-visit re-rendered with 10% head-clearance via Manus. |

## Audio

| Feature | Status | Notes |
|---|---|---|
| Audio pack | 🟢 **Live** | 55 OGG files: 24 voice clips (warm British female narrator), 13 animal sounds, 11 UI SFX, 7 music loops. Played by `AudioManager`. |
| Heavy-metal Supply Run track | 🔴 **Missing** | Brief written; not yet commissioned. Currently uses generic acoustic placeholder. |

## Game-logic + state

| Module | Status |
|---|---|
| `bond.ts`, `weather.ts`, `wardrobe.ts`, `progression.ts` | 🟢 Live + tested |
| `apprentices.ts`, `food.ts`, `rooms.ts`, `auth-validation.ts` | 🟢 Live + tested |
| `animals.ts`, `needs.ts`, `walks.ts`, `vet.ts`, `audio.ts` | 🟢 Live + tested |
| `conflicts.ts`, `calendar.ts`, `supply-runs.ts`, `depot-inventory.ts`, `depot-board.ts` | 🟢 Live + tested |
| `decorations.ts`, `time.ts`, `garden.ts`, `relationships.ts`, `visitors.ts`, `toys.ts`, `destinations.ts` | 🟢 Live + tested |
| `charity.ts`, `species-facts.ts`, `crate-stacking.ts`, `charms.ts` | 🟢 Live + tested |
| **All:** 672 tests passing | |

## Reference docs

- [`ptv-pet-transport-vehicle.md`](ptv-pet-transport-vehicle.md) — canonical PTV spec (vehicles, crates, adjacency, drive UX, cockpit, fonts, road, hedgehog crossings, all of Birchie geography).
- [`driving-systems.md`](driving-systems.md) — overview tying PTV / Supply Runs / Depot together.
- [`original-depot-supply-spec.md`](original-depot-supply-spec.md) — verbatim recovered Depot + Supply Run spec.
- [`rehoming-cast.md`](rehoming-cast.md) — 32 households + apprentices + style/personality rules.
- [`manus-sprite-rules.md`](manus-sprite-rules.md) — sprite-briefing conventions (URL refs, no-ground-context, OpenAI-only for continuity).
- [`extracted-driving-spec.md`](extracted-driving-spec.md) — historical reconstruction.
- [`future-features-lily.md`](future-features-lily.md) — Lily's wishlist + ideas backlog.
- [`gamescene-refactor-plan.md`](gamescene-refactor-plan.md) — refactor plan for the main scene.
- [`overnight-report-2026-04-24.md`](overnight-report-2026-04-24.md) — overnight session log.

## Outstanding follow-ups (known but not blocking)

1. ~~**Rename `mockup-*.html` → cleaner names**~~ ✅ Shipped 2026-04-27. All `mockup-*.html` → `*.html` and `mockup-assets/` → `scene-assets/`. AuthOverlay PAGE_URLs, InGameOverlay paths, and 35 cross-references rewritten in one commit.
2. **Wire painted dashboards** for Trikey / Bea / Big Tilly / Spark (only Henry has one). Run `tools/paint-dashboard.ts` per vehicle once cockpit-editor JSON is finalised.
3. **Real-time driving engine in Phaser** — currently the cockpit + map are mockups. Need to translate the painted dashboard + scrolling road + traffic spawner into a real Phaser scene.
4. **Adopter walking-pose wave-2** — per-person approach, not per-household groups. Plus regenerate the 22 households we paused on.
5. **Heavy-metal Supply Run track** — Manus or commissioned third-party.
6. **Charm-selection UI** — 17 painted charm sprites exist but no UI consumes them.
7. **Cast walk-by mechanic in driving** — adopters/visitors recognised in pavement crowds during PTV drives. Spec'd, no implementation.
8. **Live world map in-game** — currently `map.html` is admin-only. Wire into `InGameOverlay`.

## Cost summary (today's session)

- OpenAI gpt-image: ~$2 (apprentice walking sprites + Amara v2.1 fixes; adopter wave-1 reverted).
- Manus: ~$2k credits (mirrors, charms, cast crop regens, dashboard re-renders, road scene). Some output reverted; most banked.
- Net visible progress: cockpit + map + pre-drive mockups rebuilt with real-car layout + Birchie geography; auth flow polished and consistent; charm + mood-sprite logic in place; painted assets staged.

## "What I'd do tomorrow morning" punch-list

1. Marcus eyeballs the **painted-polish** wiring once it lands (cockpit + pre-drive show real painted vehicles + mirrors + charms).
2. Decide **rename now or later** for the production HTML.
3. Decide **next gameplay slice** — most likely: real-time PTV drive in Phaser, OR charm-selection UI, OR adopter wave-2 with per-person approach.
4. Walk through end-to-end: signup as new user → first sick animal arrives → drive-overlay vet-run flow → see charm unlock or coin reward → see dashboard polish.
