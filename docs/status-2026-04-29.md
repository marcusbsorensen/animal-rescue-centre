# A.R.C. project status — 2026-04-29

A snapshot of where every major piece sits. **Read this before deciding what to work on next.** Three columns:

- **🟢 In the live game** — wired into the Phaser scene, gameplay can hit it from a real user session.
- **🟡 Mockup-only** — built as static HTML under `apps/game/public/admin/`, design preview only, not yet wired into Phaser gameplay.
- **🔴 Missing / not started** — known design work yet to begin.

> Supersedes [`status-2026-04-25.md`](status-2026-04-25.md).

---

## What shipped between 2026-04-25 and 2026-04-29

The big themes of the four-day window:

1. **Forgot-PIN end-to-end** — design, hint capture at signup, leak-detector, idea pool, recovery-question challenge screen, server-side persistence + rate-limited fetch endpoint.
2. **First-arrival name-the-pet flow** — three-beat onboarding (greet → name → photo-to-wall) plus a "remember-this-name" recovery anchor that surfaces in forgot-PIN.
3. **Auth polish** — sticker-style PIN keypad with rotated drop-shadowed buttons; orange forgot-PIN plank; yellow back-sticker nav; name-collision sticker chips with three friendly suggestions; iPhone-viewport fixes; emoji-grid sizing fixes; admin-page rename (`mockup-*` dropped).
4. **Cast portrait re-regeneration via OpenAI** — recovered from earlier Manus style-drift; rebuilt 12 portraits + multiple variants at high quality with per-portrait style anchors. Gallery now shows variant thumbnail strips under each card.
5. **HUD weather + sundial icons** — painted icons added to disk; HUD already wired them via texture keys, so they appear on next boot.

---

## Auth + onboarding

| Feature | Status | Notes |
|---|---|---|
| Welcome screen | 🟢 **Live** | Iframe via `AuthOverlay.ts`. Painted A.R.C. logo, sleeping cat with snore-ZZZ, wagging-tail golden retriever, fox cameo, painted garden. |
| Sign-up flow | 🟢 **Live** | Three stages: name+animal picker → 4-digit PIN keypad → optional PIN-hint capture (Stage C, with NEXT button + Need-ideas helper). Sticker-style assorted-rotation keypad. Localised draft persistence (`arc-signup-draft`, version-flagged) so kids don't lose progress. |
| Login flow | 🟢 **Live** | Recent-users avatar grid as primary CTA; `Don't see your face? Type your name` as secondary fallback. PIN keypad on stage B with red-delete + green-confirm matching signup. Orange forgot-PIN plank visible on the keypad stage. |
| Name-collision suggestions | 🟢 **Live** | Server-side `suggestAvailableUsernames()` returns 3 free alternatives (`Lily1`, `LilyFox`, `BraveLily`-style variants). Surfaced in signup as painted pink/blue/mint sticker chips. |
| Forgot-PIN flow | 🟢 **Live** | `ForgotPinScene` pre-fetches saved hint via rate-limited `get-pin-hint` edge function; iframe renders Tier-1 challenge questions, then surfaces hint on 2/3 pass; Tier-3 parent-email recovery routed but not wired to actual mail-out. |
| PIN-hint capture + gut-check | 🟢 **Live** | Stage C of signup. `validatePinHint()` rejects literal PIN, 3+ digit substrings, spelled-out forms, compound forms ("one-two-three"). `getHintIdeas()` deterministic per-kid shuffle of 20 number-mappable prompts. Idea-prefilled inputs blank on first click. |
| Sign-up Tier-2 hint persistence | 🟢 **Live** | `users.pin_hint` column (migration `00004_pin_hint.sql`), enforced 4–60 char range server-side. Older accounts without a hint fall through to Tier-3. |
| First-arrival name-the-pet | 🟢 **Live** | After signup, three painted beats: greet the new pet → "what shall we call them?" → instant photo-to-wall. Recovery anchor: the chosen pet name becomes a salt for the forgot-PIN challenge later. |
| `validateAnimalName` | 🟢 **Live** | Kid-friendly pet-name filter — letters/spaces/apostrophes only, blocks profanity blocklist + reserved words. Tested. |
| Supabase auth back-end | 🟢 **Live** | Real signup/login. Username 2–18 chars, letters only, blocked-word list. Pool requirement dropped — kids can choose any safe name. PBKDF2 PIN hashing (100k iterations). Edge functions deployed: `signup`, `login`, `get-pin-hint`. |
| Auth-overlay scene-switching | 🟢 **Live** | `AuthOverlay.ts` orchestrates iframe pages via `postMessage`. Page set: welcome, login, signup, welcome-new, menu, friends, forgot-pin. |
| Yellow back-sticker nav | 🟢 **Live** | Curved-arrow SVG sticker replaces the old "Back to..." text planks across all auth screens. |

## Driving system (PTV / Supply Run)

| Feature | Status | Notes |
|---|---|---|
| First-drive vet-run flow (slice C) | 🟢 **Live** | `drive-overlay.html` intercepts the existing vet flow on the first sick animal. CTA → ~10s scripted painted drive → arrival at Bay Road Vets, hands off to existing vet popup. |
| Cockpit layout / dashboard | 🟡 **Mockup** | `cockpit.html` rebuilt to real-car layout — horn in wheel hub, speedo above wheel red-when-speeding, brake/accel pedals below, ignition (key-turn or button), hazard, dash-mounted GPS. 5 per-vehicle dashboards. |
| Per-vehicle painted art | 🟡 **Mockup** | Henry painted dashboard wired in. 5 vehicle sprites + 5 mirrors + 17 charms staged at `apps/game/public/assets/driving/`. |
| Pre-drive screen | 🟡 **Mockup** | `pre-drive.html`: destination → vehicle → mode → cargo-load → "Let's go!". |
| GPS world map | 🟡 **Mockup, blocked on art** | `map.html` rebuilt with painted Birchie geography but has serious visual issues — see [`map-representation-research.md`](map-representation-research.md). Blocked on a research-and-redesign pass before in-game wiring. |
| Cockpit slot-map editor | 🟡 **Mockup** | `cockpit-editor.html` — 11 slot bounding boxes + 2 polylines. Per-vehicle tabs with localStorage. PNG mask export ready for OpenAI in-paint. |
| OpenAI in-paint pipeline | 🟢 **Tool ready** | `tools/paint-dashboard.ts` CLI: slot-mask + per-vehicle prompt → painted dashboard. |
| Supply Run mode | 🟡 **Mockup** | Cockpit toggles to a daylight racing-fun palette (NOT neon). Music brief: heavy-metal Metallica-style — placeholder. |
| Hedgehog-crossing mechanic | 🟡 **Mockup** | Universal across PTV + Supply Run — flashing red triangle + text prompt. |
| Real-time city + fake-3D road | 🟡 **Mockup placeholder** | SVG scenery scrolls past at speed. UK two-lane road with oncoming + slow vehicles. Painted hedgerow + roadside-sprite art still needed. |
| Cargo crate-stacking puzzle | 🟢 **Logic live** | `crate-stacking.ts` engine + 32 unit tests. UI surfaces in pre-drive mockup. |
| Cargo-comfort meter + animal mood | 🟢 **Logic live** | `mirror-mood-sprites.ts` + 28 mood composites. Renders inside the mirror in cockpit mockup. |
| Charm system | 🟢 **Logic live** | `charms.ts` + 20 unit tests, 17 charms with unlock predicates. 17 painted charm PNGs delivered. UI selection panel **not yet built**. |

## Cast + adopters

| Feature | Status | Notes |
|---|---|---|
| 32 adopter household portraits | 🟢 **Live** | `apps/game/public/admin/scene-assets/cast/01–32-*.png`. After this window's regen pass, 12 of these 32 are now from OpenAI gpt-image-1.5 high (commits `569c9dd`, `62daa29`, `2fa4ce2`); the rest remain Manus originals. |
| Cast variants | 🟢 **Live** | `cast/variants/` holds painted action-pose variants per household. Estrada-greeting redone twice this window (anatomy + headcount + dark-hair fix); two-houses walking-with-dog replacing the ambiguous with-cat; Simeon-Karo greeting + with-bat-toys re-cast. |
| Cast gallery | 🟢 **Live admin** | `cast-gallery.html` shows main portrait + variant thumbnail strip under each card. Cache-busted via `?v=2026-04-28-openai`. |
| Apprentice cast (Rhubarb / Amara / Kofi) | 🟢 **Live** | Standing portraits + multiple action variants. |
| Apprentice walking poses | 🟢 **Live** | 9 sprites (front / side / waving × 3) via OpenAI with reference-portrait conditioning. |
| Adopter walking poses | 🔴 **Reverted** | Per-person approach pending for wave-2. |
| Cast static portraits used as walking-cameo placeholders | 🟢 **Live** | Per Marcus's directive — cameos use existing standing portraits until wave-2. |
| 4 cropped portraits regen | 🟢 **Live** | `07-patel-greens` / `12-odenkirks` / `28-iris-teacher` / `17-benji-wild-visit` re-rendered with 10% head clearance. |

## HUD + UI

| Feature | Status | Notes |
|---|---|---|
| Weather + sundial icons | 🟢 **Live** | 8 weather PNGs + sundial committed `f6288c1`. HUDView already wired the texture keys; asset-manifest plugin auto-classifies tier-boot, BootScene picks them up. Replaces the Unicode glyph fallback. |
| Weather particles | 🟢 **Live** | `WeatherParticles.ts` overlay — rain, snow, fog. |
| HUD time-of-day strip | 🟢 **Live** | `[sundial]  Morning  ▓▓▓░░  [☁]  Cloudy` second row. |

## Audio

| Feature | Status | Notes |
|---|---|---|
| Audio pack | 🟢 **Live** | 55 OGG files: 24 voice clips (warm British female narrator), 13 animal sounds, 11 UI SFX, 7 music loops. Played by `AudioManager`. |
| Speaker-icon music toggle | 🟢 **Live** | Iframe pages post `toggle-music`; AuthOverlay echoes back the new state so the icon flips between 🔊 and 🔇. |
| Heavy-metal Supply Run track | 🔴 **Missing** | Brief written; not yet commissioned. Currently uses generic acoustic placeholder. |

## Game-logic + state

| Module | Status |
|---|---|
| `bond.ts`, `weather.ts`, `wardrobe.ts`, `progression.ts` | 🟢 Live + tested |
| `apprentices.ts`, `food.ts`, `rooms.ts`, `auth-validation.ts` | 🟢 Live + tested |
| `animals.ts`, `needs.ts`, `walks.ts`, `vet.ts`, `audio.ts` | 🟢 Live + tested |
| `conflicts.ts`, `calendar.ts`, `supply-runs.ts`, `depot-inventory.ts`, `depot-board.ts` | 🟢 Live + tested |
| `decorations.ts`, `time.ts`, `garden.ts`, `relationships.ts`, `visitors.ts`, `toys.ts`, `destinations.ts` | 🟢 Live + tested |
| `charity.ts`, `species-facts.ts`, `crate-stacking.ts`, `charms.ts` | 🟢 Live + tested |

## Reference docs

- [`ptv-pet-transport-vehicle.md`](ptv-pet-transport-vehicle.md) — canonical PTV spec.
- [`driving-systems.md`](driving-systems.md) — PTV / Supply Runs / Depot overview.
- [`original-depot-supply-spec.md`](original-depot-supply-spec.md) — verbatim recovered Depot + Supply Run spec.
- [`rehoming-cast.md`](rehoming-cast.md) — 32 households + apprentices + style/personality rules.
- [`manus-sprite-rules.md`](manus-sprite-rules.md) — sprite-briefing conventions (URL refs, no-ground-context, OpenAI-only for continuity).
- [`forgot-pin-recovery.md`](forgot-pin-recovery.md) — three-tier recovery design.
- [`future-features-lily.md`](future-features-lily.md) — Lily's wishlist + ideas backlog.
- [`map-representation-research.md`](map-representation-research.md) — **NEW** research task before any further map work.
- [`gamescene-refactor-plan.md`](gamescene-refactor-plan.md) — refactor plan for the main scene.
- [`overnight-report-2026-04-24.md`](overnight-report-2026-04-24.md) — overnight session log.

## Outstanding follow-ups (known, not blocking)

1. ~~**Rename `mockup-*.html`**~~ ✅ Shipped 2026-04-27.
2. **Wire painted dashboards** for Trikey / Bea / Big Tilly / Spark (only Henry has one). Run `tools/paint-dashboard.ts` per vehicle once cockpit-editor JSON is finalised.
3. **Real-time driving engine in Phaser** — translate painted dashboard + scrolling road + traffic spawner into a real Phaser scene.
4. **Adopter walking-pose wave-2** — per-person approach, not per-household groups. Plus regenerate the 22 paused households.
5. **Heavy-metal Supply Run track** — Manus or commissioned third-party.
6. **Charm-selection UI** — 17 painted charm sprites exist but no UI consumes them. Mockup HTML being drafted at `apps/game/public/admin/charm-select.html` (in progress 2026-04-29).
7. **Cast walk-by mechanic in driving** — adopters/visitors recognised in pavement crowds during PTV drives. Spec'd, no implementation.
8. **Live world map in-game** — **BLOCKED** on map representation research (see [`map-representation-research.md`](map-representation-research.md)). Don't wire `map.html` into `InGameOverlay` until the new treatment lands.
9. **Tier-3 parent-email recovery** — UI routed in `forgot-pin.html`, but no actual email-out wired. Either commission via SES/Resend or drop the tier and retitle.
10. **PIN-hint length cap (60 chars)** — Lily's "Danish numbers (the language no-one speaks here)" was 73 chars and had to be trimmed to 49. Consider bumping the server cap to 80–100 chars.
11. **Adoption matching mini-game** — design doc landing 2026-04-29 at `docs/adoption-matching.md`. First gameplay slice for the "where do animals go" loop.

## Cost summary (this window)

- OpenAI gpt-image-1.5 high: **~$5** for 30+ regenerations (12 cast portraits + ~6 variants + 8 retries on Estrada/Simeon style/headcount fixes).
- Manus: 0 credits — held off after the earlier style-drift incident; OpenAI is now the sole pipeline for continuity-critical edits.

## "What I'd do next morning" punch-list

1. Eyeball the new Estrada-greeting variant at full size — confirm headcount (6 brothers + 2 sisters) and dark-hair direction held.
2. Decide whether to keep building the **charm-selection UI** as a polished mockup, or jump to **adoption-matching mini-game** prototype.
3. Map research — do the comparable-game scan from `map-representation-research.md` so the map can come off blocked status.
4. PIN-hint cap bump — small back-end change + redeploy edge function.
5. Walk through end-to-end: new signup → name the pet → first sick animal arrives → drive-overlay vet-run flow → first hedgehog brake → see "First Vet Run" charm unlock toast.
