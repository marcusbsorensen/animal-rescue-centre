# A.R.C. project status — 2026-07-04

Reconciled snapshot after a six-week dormancy (last commit 22 May 2026).
**Supersedes [`status-2026-04-29.md`](status-2026-04-29.md)**, which is now badly
out of date — much of what it listed as "mockup" or "proposal" has since shipped.

Health at time of writing: **typecheck clean, 786 tests passing** across 5
workspace projects. Nothing rotted during dormancy.

---

## What actually shipped since the 04-29 status doc

The 04-29 doc and the design docs predate a large body of work. Verified live
in code today:

- **Native adoption mini-game** — `AdoptionMatchScene.ts` (580 lines), wired
  into GameScene (~line 1047). The 04-29 doc lists this as an unbuilt proposal.
- **Garden tunnel minigame, tiers 1–5** — real templates for fox, hedgehog,
  raccoon, skunk + multi-animal tier 5 in `packages/game-logic/src/tunnel.ts`.
- **Charm-selection scene**, **kitchen minigame**, **supply-run scene**
  (`SupplyRunScene.ts`, pseudo-3D road), **social scene**.
- **Seasonal world map** — 4-season cross-fade — and it *is* wired into the
  in-game overlay (`map.html?embed=1` in `InGameOverlay.ts`).
- **Intro zoom sequence**, **corridor left-rail control panel**.
- **GameScene refactor** — the store/view-module extraction from
  `gamescene-refactor-plan.md` is substantially done: GameScene down to
  2,275 lines, `game-state/` + `game-views/` (15 modules) exist, the
  stale-closure-after-resize bug is fixed.
- **Educational content** — `species-facts.ts`, 40+ facts shown on arrival.

## The one genuinely big unbuilt system

- **Real-time driving in Phaser.** All the *logic* exists and is tested
  (`crate-stacking.ts`, `charms.ts`, mirror-mood sprites, `destinations.ts`).
  What is missing is the Phaser presentation layer — top-down travel, cab
  events, and the camera-dive transition. Only the scripted vet-run "slice C"
  (`drive-overlay.html`) is mounted; the rich `cockpit.html` (2,613 lines) and
  `pre-drive.html` (897 lines) are mockups mounted nowhere.
  See [`plan-driving-engine-2026-07-04.md`](plan-driving-engine-2026-07-04.md).

## Fresh plans (2026-07-04)

Four execution-ready scoping docs written this session:

| Stream | Doc | Verdict |
|---|---|---|
| Adoption dialogue | [`plan-adoption-dialogue-2026-07-04.md`](plan-adoption-dialogue-2026-07-04.md) | Tier 1 ~1.5–2 days, **zero art commission**, non-breaking behind a flag. |
| Driving engine | [`plan-driving-engine-2026-07-04.md`](plan-driving-engine-2026-07-04.md) | ~11–15 half-day sessions, 6 slices. The major build. |
| GameScene refactor | [`plan-gamescene-refactor-2026-07-04.md`](plan-gamescene-refactor-2026-07-04.md) | **Already serviced.** ~6–9h optional, no player payoff. Extract on contact. |
| New content for Lily | [`plan-lily-content-2026-07-04.md`](plan-lily-content-2026-07-04.md) | Make hedgehog a real adoptable species at L7. ~½ day + sprites. |

## Real bugs surfaced this session (independent of the above)

1. **Adopter avatar paths mismatch.** `avatarSrc` in `adoption.ts`
   (e.g. `cast/01-pri-kaur.png`) does not match files on disk
   (`cast/01-priya.png`). Adopter faces likely render as painted-initials
   fallback, not portraits. Cheap fix; worth doing regardless of dialogue work.
2. **Two adjacency bonuses unwired** — sibling-pair and dog-near-recovering
   `calculateArrivalHappinessDelta` TODOs remain stubbed.
3. **Stale progression doc.** `level-progression-overview-2026-05-03.md` has
   three material errors: capacity is not capped at 12 (runs to 30); Sea Cliffs
   moved L7→L6 (so L6 is busy, not empty); the "empty level" is actually **L7**;
   and hedgehog/raccoon/skunk/squirrel are puzzle labels, not real species yet.

## Still-open loose ends (from 04-29, still valid)

- Tier-3 parent-email PIN recovery — routed, no mail-out (wire Resend or drop).
- 4 remaining painted vehicle dashboards (blocked behind the driving decision).
- Heavy-metal Supply Run track — placeholder in use.
- Adopter walking-pose wave-2 — reverted; per-person regen pending.

## Recommended sequence

1. **Fix the avatar-path bug** (minutes) — visible defect, no dependencies.
2. **Adoption dialogue Tier 1** (~1.5–2 days) — high delight, no art spend, gentle
   re-entry ramp after dormancy. Makes a shipped feature sing.
3. **Hedgehog as a real species** (~½ day + sprites) — fills the one dead level,
   closes a continuity gap (Lily has already met the hedgehog in the tunnel).
4. **Driving engine** — the deliberate multi-week arc. Start with a dive-transition
   prototype against a placeholder cab to de-risk *before* commissioning art.
5. **Refactor** — do not schedule; extract on contact.
